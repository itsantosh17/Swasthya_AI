# Swasthya AI (project01)

## Run (backend + web)

1. Install Node.js (v18+ recommended)
2. In this folder run:
   - `node server.js`
3. Open:
   - `http://localhost:3000`

## Notes

- This backend stores data in `data/db.json` (demo only; passwords are stored in plain text).

